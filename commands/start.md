---
description: Start a tracked task in the logbook (new task from a description)
argument-hint: [task description]
---

Invoke the `logbook` skill to start a new tracked task. Task description: $ARGUMENTS

If $ARGUMENTS is empty, ask the user what they want to start tracking — don't pick from the queue (that's `/logbook:next`).
