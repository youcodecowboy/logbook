---
description: Pick up the next queued task and start working on it
---

Invoke the `logbook` skill: pick up the next queued task and start working on it. Use the "next queued" selection rule (priority desc, then date asc within same priority — see the main skill for details). Move it from `queued/` to `active/`, update its `Status:` and the `index.md` row, then begin executing the Plan.
